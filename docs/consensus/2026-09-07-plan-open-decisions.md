# 2026-09-07 — PLAN.md 열린 결정 7건

> 각 항목의 **답변:** 줄에 `A`/`B`/`C`… 또는 자유롭게 적어주세요.
> 적힌 대로 코드·PLAN.md·CLAUDE.md 에 반영하고, 이 파일 상단에 (해결됨) 표시합니다.
> 상태: **해결됨 · 2026-09-07** (7/7) — 반영 완료.
> ⚠️ **D1·D2(이름 변경)는 이후 사용자가 원복.** 스튜디오 이름은 Motion / Look / Voice Studio 유지.

## 확정 요약

| ID | 결정 |
|---|---|
| D1 | ~~`/audio` 이름 = Face-Reading AI~~ → **취소, Voice Studio 유지** |
| D2 | ~~`/video` 이름 변경~~ → **취소, Motion Studio 유지** |
| D3 | 자유 프롬프트 입력창 유지. 컨셉 버튼을 누르면 정제 프롬프트가 그 입력창에 자동으로 채워짐 |
| D4 | 기본값 **720p · 10초**. 하단 선택창에서 360/720/1080p, 길이 슬라이더(최소 3초 — Omni 하한 — ~10초) 조절 가능 |
| D5 | 동의 팝업 = **매 진입마다** (`/video`·`/image`) |
| D6 | GCS 수명주기 규칙 **없음.** 스태프가 수동 삭제. 동의 문구는 "행사 종료 후 삭제" |
| D7 | 갤러리 = 01·02 만. 03 제외 |

---

## D1. Voice Studio(`/audio`) 새 이름

체험 = AI 관상가 아주머니가 웹캠으로 얼굴 보며 실시간 음성으로 관상.
"Studio" 안 써도 됨, 직관적인 이름 원함.

- **A. AI 관상소** — 부스·한국어 관객에 가장 직관적. 아주머니 페르소나와 결이 맞음 *(추천)*
- **B. 관상 라이브** — "실시간"을 전면에
- **C. 오늘의 관상** — 가볍고 재미 위주
- **D. Face Reader** — 01·02 영어 이름(Short-Form Studio / AI Closet)과 통일감
- **E. 직접 입력**

**답변:** Face-Reading AI

---

## D2. "Short-Form Studio"(`/video`)에서 "Studio" 뗄지

`/image` 는 "AI Closet" 으로 "Studio" 를 뗐음. `/video` 도 맞출지.

- **A. "Short-Form Studio" 유지**
- **B. "숏폼" 으로**
- **C. "AI 쇼츠" 로** *(D1 을 "AI 관상소"로 할 경우 "AI ○○" 짝이 맞음 — 추천)*
- **D. "Short Form" (Studio만 제거)**
- **E. 직접 입력**

**답변:** Short-Form Studio로 유지

---

## D3. Short-Form — 자유 프롬프트 입력 노출

컨셉 버튼(놀이공원/시상식/레드카펫)이 기본 흐름. 자유 텍스트 입력창을 어떻게 할지.

- **A. 완전히 숨김** — 컨셉 버튼만. 부스에서 가장 단순 *(추천)*
- **B. 스태프용 토글 뒤에 숨김** — 평소 안 보이고 작은 "직접 입력" 버튼으로만 펼침
- **C. 지금처럼 노출** — 컨셉 버튼 + 자유 입력 둘 다 항상

**답변:** C. 컨셉 버튼을 누르면 자동으로 지금 입력창에 적히게끔 함

---

## D4. Short-Form — 해상도·길이 고정값

부스는 방문객이 반복해서 누름. 생성 1회 비용이 여기서 정해짐.
(720p = $0.10/초, 360p ≈ $0.033/초, 9:16 세로 고정 가정)

- **A. 720p · 5초** (생성당 약 $0.50) — 화질 "와" 포인트 유지 *(추천)*
- **B. 720p · 8초** (약 $0.80)
- **C. 360p · 5초** (약 $0.17) — 비용 최소, 화질은 초안 수준
- **D. 컨셉마다 다르게** (예: 레드카펫만 8초)
- **E. 직접 지정**

**답변:** 720 10초로 default값을 놔둬주고, 지금처럼 하단에 내려오는 선택창으로 360 720 1080, 0초~10초 선택할 수 있게 해줘

---

## D5. 동의 팝업 표시 빈도

`/video`·`/image` 진입 시 "체험 콘텐츠는 갤러리에 잠시 전시 후 행사 종료 시 전부 삭제" 안내 + 동의.

- **A. 세션당 1회** — `sessionStorage`. 스태프가 새로고침으로 리셋 *(추천)*
- **B. 매 진입마다** — 방문객이 바뀔 수 있으니 항상
- **C. 부스별 1회** — /video 에서 동의하면 /image 도 통과 (반대도)

**답변:** B

---

## D6. GCS 버킷 수명주기 (동의 문구와 일치해야 함)

`gs://smproject-sh` 오브젝트 자동 삭제 주기. 콘솔에서 사용자가 설정.
동의 팝업 문구를 여기 맞춰 씀.

- **A. 1일** — 매일 자정 이전 것 삭제. 문구: "다음 날 삭제"
- **B. 행사 다음날 일괄(2026-09-15)** — 부스 당일은 전부 보존, 하루 뒤 수동/규칙 삭제 *(추천)*
- **C. 7일**
- **D. 30일** (기존 CLAUDE.md 에 적혀 있던 값)
- **E. 직접 지정**

**답변:** 수동으로 삭제할거임

---

## D7. 갤러리에 03(관상) 결과 포함?

03 은 음성 체험이라 저장되는 시각 결과물이 없음.

- **A. 제외** — 01·02 사진/영상만 *(추천 · 자연스러움)*
- **B. 포함** — 관상 세션 자막/요약을 카드로라도

**답변:** A
